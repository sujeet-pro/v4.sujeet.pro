# LLM Code Guidelines (Canonical)

This folder is the single source of truth for **codebase changes** (TypeScript, CSS, accessibility, performance).

Use:

- coding-standards.md

Content work should use `llm_docs/guidelines-content/` instead.
