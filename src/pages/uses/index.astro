---
import Item from "@/components/item.astro"
import Link from "@/components/link.astro"
import Layout from "@/layout/layout.astro"
import { getCategoriesForType } from "@/utils/content-categories-generic.utils"
import { getUses } from "@/utils/content-uses.utils"
import type { ContentItemWithoutContent } from "@/utils/content.type"

const uses = await getUses()
const itemsWithoutContent = uses.map(({ Content: _, ...rest }) => rest) as ContentItemWithoutContent[]
const categories = await getCategoriesForType("uses", itemsWithoutContent)
const crumbs = null
---

<Layout title="Uses" description="Setup, tools, and productivity systems" {crumbs} pageType="default">
  <h1 class="page-title">Uses</h1>
  <p class="page-description">Setup, tools, and productivity systems I use daily.</p>

  <!-- Categories -->
  <section class="mb-12">
    <h2 class="section-title">Browse by Category</h2>
    <div class="grid gap-3 md:grid-cols-2 lg:grid-cols-3">
      {
        categories.map((cat) => (
          <Link href={cat.href} class="category-card">
            <h3 class="category-card-title">{cat.name}</h3>
            <p class="category-card-count">{cat.items.length} {cat.items.length === 1 ? "article" : "articles"}</p>
          </Link>
        ))
      }
    </div>
  </section>

  <!-- All Entries -->
  <section>
    <h2 class="section-title">All Articles</h2>
    {
      uses.length > 0 ? (
        <ul class="flex flex-col">
          {uses.map((item) => (
            <Item item={item} />
          ))}
        </ul>
      ) : (
        <p class="text-muted">No entries yet. Check back soon!</p>
      )
    }
  </section>
</Layout>
