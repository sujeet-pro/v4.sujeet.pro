---
interface Props {
  title: string
  as?: "article" | "section"
}

const { as } = Astro.props as Props
const Element = as === "article" ? "article" : "section"
---

<Element class="mx-auto my-8 prose prose-base max-w-none">
  <slot />
</Element>
